export const selectIsDraw = ({ isDraw }) => isDraw;

export const selectCurrentPlayer = ({ currentPlayer }) => currentPlayer;

export const selectGetField = ({ getField }) => getField;

export const selectIsGameEnded = ({ isGameEnded }) => isGameEnded;
